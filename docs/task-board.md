# 任务面板

## 当前目标

把项目从“数据扫描骨架”推进到“可配置白板小人战斗分析器”的第一版。

## V1 决策

按最新范围约定：

- `V1` 只支持 `RimWorld` 原版与已安装 DLC
- `V1` 优先完成“原版武器、护具、基础战斗分析”的可用流程
- `Mod` 导入不是取消，而是延后为 V1 之后的扩展功能
- 只有在原版分析流程已经可实际使用、可验证、可分享后，才开始扩展 Mod 支持

## 当前范围

第一版聚焦以下能力：

- 定义攻击方与防守方白板小人
- 支持基础种族基线：默认 `RimWorld` 原版智人
- 支持部分原版特性对战斗的影响
- 支持衣物/护甲叠穿合法性检查
- 支持指定身体区域上的多层护甲结算
- 支持基础远程与近战命中率、单次期望伤害、DPS 计算
- 支持从 JSON 场景文件运行分析

## 暂不纳入

- 完整解析原版与 Mod 的全部武器、服装、身体部位 Def
- 全身命中部位随机分布与器官穿透细节
- Combat Extended 等重写战斗系统的 Mod
- 战斗逐 tick 模拟与蒙特卡洛模拟

## 里程碑

### 里程碑 1：战斗分析最小可用版

状态：进行中

交付内容：

- `analyze-scenario` CLI
- 白板小人、武器、护具、修正器数据模型
- 多层护甲期望结算
- 远程/近战基础期望输出
- 示例场景
- 单元测试

### 里程碑 2：原版 Def 接入

状态：待开始

交付内容：

- 从原版 XML 读取武器与护具
- 生成标准化装备目录
- 把装备目录接入场景分析器

### 里程碑 3：V1 原版分析流程闭环

状态：进行中

交付内容：

- 原版武器/护具目录导出
- 基础武器 vs 护具分析表
- 一套明确的 CLI 使用流程
- 一份可直接展示在 GitHub 的样例输出
- 场景库、模板继承、横向对比表
- 静态 HTML 对比页

当前进度：

- 已完成原版导入、场景库、模板继承、CSV/JSON 对比输出
- 已完成静态 HTML 对比页
- 已完成场景库 `V1` 格式定稿、文档化与加载期校验
- 已完成桌面工作台入口，可在应用界面中执行核心分析流程
- 已完成中文桌面界面与单场景表单编辑器
- 已完成面向普通用户的桌面端改版：
  - 顶部大块开发中头图已压缩为紧凑工具条
  - 常驻大日志区已改为“查看日志”弹窗
  - 场景编辑页已改为以按钮、勾选框、列表、下拉框为主的操作流
  - 武器支持按远程/近战分类点选
  - 护具支持从原版目录中直接点选添加，不再要求手工填写名称
  - 高级手工字段仍保留，但已收起到“高级设置”
- 已完成场景库页的普通用户化改版：
  - 支持先读取场景库内容，再显示标签和场景列表
  - 支持通过勾选标签、选择场景进行批量分析
  - 普通用户不再需要手工输入场景 ID
- 已新增射击测试场景库：
  - 新增 `shooting-test-library.json`
  - 包含 12 个射手组合、3 个靶子模板、36 个测试场景
  - 桌面端已增加“载入射击测试库”快捷入口
  - 为表达“射击指令额外增加射击等级”等条件，已补充 `shooting_skill_offset` 修正字段
- 已完成 README 的中文桌面端使用说明，补充了虚拟环境启动方式和 `rim-analysis` 命令不可用时的直接启动方式
- 当前桌面端验证状态：
  - `python -B -m pytest` 通过，`25 passed`
  - GUI 初始化检查通过
  - 场景库页读取样例库初始化通过
- 下一步重点转为：
  - 继续把“包扫描”和“原版装备目录”页按普通用户界面标准压缩和重排
  - 增强原版导入字段与统计字段
  - 视需要补充桌面端交互测试

### 里程碑 4：Mod Def 接入

状态：待开始

交付内容：

- 从 Workshop 与本地 Mod 解析武器与护具
- 兼容常见 Def 继承与 Patch 后结构
- 支持“原版 + 指定 Mod 集”分析
